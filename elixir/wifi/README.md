# Wifi

AtomVM Elixir Wifi Examples

## WifiAP

Launches an access point that can be connected to

```sh

mix atomvm.esp32.flash --port /dev/tty.usbmodem2101
```


## WifiSTA

Connects to existing access point

```sh
mix atomvm.esp32.flash --port /dev/tty.usbmodem2101
```